export enum OrderState {
    draft = 'draft',
    waitingConfirmation = 'waiting for confirmation',
    confirmed = 'confirmed',
    inProgress = 'in progress',
    completed = 'completed',
    canceled = 'canceled'
}